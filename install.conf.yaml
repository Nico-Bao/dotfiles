- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#     ~/.vim: vim
#     ~/.vimrc: vimrc
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Work

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
  - echo "hey you guys"
  - sleep 2
  -  #testing running an external script file
    command: ./test.zsh
    stdout: true  #to allow output
    stderr: true  #to show eventual errors
